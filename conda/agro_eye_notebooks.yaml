name: agro_eye_notebooks
channels:
  - anaconda
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - python-dotenv
  - pandas
  - pip
  - pip:
    - numpy 
    - requests
    # packages for fecthing data from openmeteo
    - openmeteo-requests
    - requests-cache 
    - retry-requests 
    - matplotlib
    - plotly
    - nbformat

